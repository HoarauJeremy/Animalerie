{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <ul>
        
        {% for product in products %}
            <li>{{ product.name }} - {{ product.price }} € | {% if product.isActive %} Disponible {% else %} Indisponible {% endif %} 
            {# <li>{{ product.name }} - {{ product.price|format_currency('EUR') }} | {% if product.isActive %} Disponible {% else %} Indisponible {% endif %}  #}
                <ol>
                {% if product.petShops is not empty %}
                    {% for petshop in product.petShops %}
                       <li>Animalerie: {{ petshop.name }}</li>                        
                    {% endfor %}
                {% endif %}
                </ol>
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
